export const HTTPS_API_URL = 'https://api.vinorajan.xyz';

export const LOCAL_URL = 'http://localhost:9000';

export const LOGIN_URL = '/api/login';

export const getUrl = (url) => {
    return HTTPS_API_URL + url;   
}